// Compatibility re-export for modules that still import the legacy path helper.
export {
  canonicalizePath,
  isCanonicalPathInside,
  resolveCanonicalPath,
} from './deployment-policy.service.js';
